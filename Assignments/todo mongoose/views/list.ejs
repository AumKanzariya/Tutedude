<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title>todo</title>
</head>
<body>
    <div class="box">
        <h1>Todo List</h1>
        
    </div>

    <div class="box">
        <% dayej.forEach(function(item){ %>
            <form action="/delete" method="post">
                <div class="item">
                    
                    <input class="task-checkbox" name="checkbox1" value="<%=item._id%>" onchange="this.form.submit()" type="checkbox">
                    <p class="task-text"><%= item.name %></p>
                </div>
            </form>
       <% }); %>
    </div>
        <form action="/" method="POST">
            <input type="text" name="a1" placeholder="enter the task" autofocus>
            <button type="submit">+</button>
        </form>
</body>
</html>